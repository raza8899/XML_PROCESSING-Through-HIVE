# XML_PROCESSING-Through-HIVE
In this XMLSerDE is used for Xml data processing,Hive managed table is used and xml data is loaded from LFS.To execute hql script use "hive -f FileName.xml" from terminal in a hadoop environment.
